#ifndef TTHREADS_HPP
#define TTHREADS_HPP

#include <pthread.h>

/*
class tThreads {
public:
    tThreads(int aff, void *p);
    int tJoin();
private:
    static void* tT(void*);
    pthread_t m_t;
};
*/

#endif // TTHREADS_HPP
